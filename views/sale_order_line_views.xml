<?xml version="1.0" encoding="UTF-8"?>
<!--odoo>
    <record id="view_order_line_tree_inherit_brand_ncm" model="ir.ui.view">
        <field name="name">sale.order.line.tree.inherit.brand.ncm</field>
        <field name="model">sale.order.line</field>
        <field name="inherit_id" ref="sale.view_order_line_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='product_uom_qty']" position="after">
                <field name="brand_id" string="Marca"/>
                <field name="ncm_id" string="Ncm"/>
            </xpath>
        </field>
    </record>

</-odoo>
<odoo>
     Extensão da View 'sale.order.line' ->
    <record id="view_sale_order_line_form_brand_ncm" model="ir.ui.view">
        <field name="name">sale.order.line.form.brand.ncm</field>
        <field name="model">sale.order.line</field>
        <field name="inherit_id" ref="sale.view_order_line_form"/>
        <field name="arch" type="xml">
            <!- Adicionando o campo brand_id e ncm_id após o campo product_id ->
            <xpath expr="//field[@name='product_id']" position="after">
                <field name="brand_id" string="Marca" />
                <field name="ncm_id" string="NCM" />
            </xpath>
        </field>
    </record>
</odoo-->

<odoo>
    <record id="l10n_br_sale_order_line_tree_inherit" model="ir.ui.view">
        <field name="name">l10n_br.sale.order.line.tree.inherit</field>
        <field name="model">sale.order.line</field>
        <field name="inherit_id" ref="sale.view_order_line_tree"/> <!-- Ref para a tree view de sale.order.line -->
        <field name="arch" type="xml">
            <!-- Adiciona as novas colunas na tree de sale.order.line -->
            <xpath expr="//tree[@model='sale.order.line']" position="inside">
                <field name="ncm_id"/>
                <field name="brand_id"/>
            </xpath>
        </field>
    </record>
</odoo>
